import React from 'react';
import ReactDOM from'react-dom/client';
import { GifExpertApp } from './GifExpertApp';
import "./index.css";

const divRoot = document.querySelector('#root');
ReactDOM.createRoot(divRoot).render(< GifExpertApp />)
